{
  "name": "@latexmk/dashboard",
  "version": "0.2.0",
  "private": true,
	"type": "module",
  "scripts": {
	"dev": "vite",
	"build": "tsc --noEmit && vite build",
	"test": "tsc --noEmit",
	"lint": "tsc --noEmit",
	"clean": "rm -rf dist"
	},
	"dependencies": {
		"@preact/signals": "^2.9.2",
		"lucide-preact": "^0.468.0",
		"preact": "^10.26.4"
	},
	"devDependencies": {
		"@preact/preset-vite": "^2.10.1",
		"typescript": "^5.7.3",
		"vite": "^6.2.0"
	}
}

{
  "name": "@latexmk/cli",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('dist',{recursive:true})\" && go build -trimpath -o dist/latexmk ./cmd/latexmk",
    "test": "cross-env GOWORK=off go test ./...",
    "lint": "cross-env GOWORK=off go tool -modfile=../../tools/go.mod golangci-lint run --config ../../.golangci.yml ./...",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "format": "cross-env GOWORK=off go tool -modfile=../../tools/go.mod golangci-lint fmt --config ../../.golangci.yml ./...",
    "format:check": "cross-env GOWORK=off go tool -modfile=../../tools/go.mod golangci-lint fmt --config ../../.golangci.yml --diff ./...",
    "test:race": "cross-env GOWORK=off go test -race ./..."
  }
}
